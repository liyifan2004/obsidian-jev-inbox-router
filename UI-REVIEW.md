# JEV Inbox Router · UI/UX 评审

**评审对象**：`obsidian-jev-inbox-router` v0.1.0（源码评审，未在 Obsidian 实机运行）
**评审范围**：`styles.css`、`src/modals.ts`、`src/settings-tab.ts`、`src/main.ts`（状态栏）、`src/note-writer.ts`（笔记内判断块）
**评审分支**：产品分支（交互界面），模式 = 生产力／开发工具伴随界面（密度优先、静默优先、原生外观）
**评审时间**：2026-09-21

## 结论

**NOT PASS**（硬门槛 2 项不通过）

| | |
|---|---|
| 原始分 | **56.5 / 100** |
| 硬门槛封顶 | 59（未触及） |
| 有效分 | **56.5 / 100** |
| 默认准入线 | 70 |
| 目标（本次要求：极简高审美） | 80+ |

方向不是错的——文案诚恳、安全边界清晰、状态与错误处理齐全，这是很多插件做不到的。扣分几乎全部集中在**视觉系统尚未成立**：颜色把「标识」当成「文字色」用，信息层级被摊平成 6 个等权卡片，控件有相当一部分不是真控件。修完 P0/P1 后能否回到 80 分区间，主要取决于「一屏只有一个主角、颜色只做标识」这两条纪律是否被执行。

**证据上限**：本次为静态源码 + 计算式对比度评审，未在 Obsidian 内渲染，未做键盘走查与真机触摸测试。所有对比度数字为脚本实算（`WCAG 2.x` 相对亮度公式），可在 `design-preview.html` 中切换主题目视复核。任何标注「待实测」的条目在上调评分前必须先跑一遍实机。

---

## 一、硬门槛

| 门槛 | 结果 | 证据 |
|---|---|---|
| 核心任务可完成 | PASS | 判断 → 移动 → 撤销链路完整；双门槛只标记不移动的逻辑可读 |
| 遮挡 / 裁切 / 溢出 | PASS | 弹窗内容无固定高度；概率行有 ellipsis 兜底（另见 P1-3 字号缩放风险） |
| **文字与图形对比度** | **FAIL** | 亮色主题下分类色被当作 11px 文字色使用：D 1.58:1、E 2.06:1、B 2.35:1、C 2.77:1、F 3.03:1、A 3.20:1（要求 4.5:1）。同一批颜色作为图形（圆点／进度条）也全部低于 3:1 |
| **键盘与触摸** | **FAIL** | 文件夹／分类选择行是 `div + onClickEvent`，无 `role`／`tabindex`／焦点样式；行高 38px（`padding:9px 12px` + 13px 文字），低于 44px；`isDesktopOnly: false` 说明移动端在范围内 |
| 加载 / 空 / 成功 / 错误 / 恢复状态 | 部分 FAIL | 加载（`测试中…`、扫描进度 Notice）、成功、错误都有；**分类选择器无空状态**（全部停用后弹窗全空白）；**自动分流后没有就地撤销入口** |
| 破坏性动作标识对象与后果 | 部分 FAIL | 按钮有 `desc` 说明，但「恢复默认分类」的 desc 完全缺失，且两者都无二次确认、无撤销 |
| 标识／字体／图标版权 | N/A | 无自带字体与图片；图标全部走 Obsidian 内置 Lucide，未使用 emoji 冒充标识 |
| 数据与宣称真实性 | PASS | 未发现占位数据、虚假指标或未经证实的宣称 |

---

## 二、评分维度

| 维度 | 权重 | 评分 | 得分 | 主要证据 | 单点最高收益改动 |
|---|---:|---:|---:|---|---|
| 产品目标与 UX | 20 | 3.5 | 14.0 | 主路径完整、`dryRun` 与双门槛设计克制；恢复路径只有命令面板 | 自动分流后给一个就地撤销入口 |
| 信息架构与布局 | 15 | 3.0 | 9.0 | 设置页 6 节顺序合理；但三个重置按钮并排、展开状态无标识 | 重置类动作收进「⋯」菜单并加确认 |
| 视觉层级与留白 | 15 | 2.5 | 7.5 | 结论条／元信息卡／进度条轨道共用 `--background-secondary`，无主角 | 结论行去底色，元信息压成一行键值 |
| 字体排印 | 10 | 3.0 | 6.0 | 用主题角色与等宽字体处理数据，正确；但 `.jev-prob-row` 列宽写死 px，不随字号设置缩放 | 列宽改 `max-content` / `minmax` |
| 颜色 | 10 | 1.5 | 3.0 | 亮色主题 6 色全部不达标；6 种饱和色同时出现在一张图上 | 分类色只做标识，且按主题折算 |
| 组件与图标 | 10 | 2.5 | 5.0 | 图标沿用 Lucide、设置用 Obsidian `Setting` API（好）；但裸 `input`、裸 `checkbox`、div 当按钮 | 选择器改 `SuggestModal`，开关改 `addToggle` |
| 内容与反馈 | 10 | 4.0 | 8.0 | 文案是本项目最强项：说明后果、不夸大、错误可行动 | 主按钮点名对象（「移动到 要做的事」） |
| 响应式与可访问性 | 10 | 2.0 | 4.0 | 移动端在范围内，但触摸目标、键盘焦点、`prefers-reduced-motion` 均未处理 | 行高 44px + 焦点环 + 动效降级 |
| **合计** | **100** | | **56.5** | | |

---

## 三、发现清单

### P0（硬门槛不通过）

**P0-1｜分类色被当作文字色，亮色主题全部不达标**
现象：`modals.ts:89` 把 `r.color` 直接赋给概率行左侧字母的 `color`，字号 `--font-ui-smaller`（约 11px）。
影响：亮色主题下 D（待办，默认色）只有 **1.58:1**，用户几乎看不见字母；同时 `.jev-dot` 与 `.jev-prob-fill` 也用同一批颜色，作为图形需要 3:1，D 1.58／E 2.06／B 2.35／C 2.77 全部不足 → 「这条笔记属于哪一类」这个唯一信号在最差的组合下消失。
标准：WCAG 2.x 正文 4.5:1、必需图形 3:1（`quality-gates.md` 硬门槛）。
修改：分类色退出文字层，只保留在「≥8px 标识点」和「被选中项的进度条」；两者统一走主题折算
`color-mix(in srgb, var(--jev-cat) 55%|85%, var(--text-normal))`（亮 55% / 暗 85%，实算后亮色 3.32~5.83:1，暗色 5.92~11.68:1）。字母键与所有正文回归 `--text-normal` / `--text-muted`。
复测：在 `design-preview.html` 切亮色主题逐行读；对每个默认色重新跑对比度脚本，图形位 ≥3:1。

**P0-2｜选择行不是控件，键盘不可达且触摸目标不足**
现象：`modals.ts:158-169`（分类选择）与 `modals.ts:210-215`（文件夹选择）用 `div + item.onClickEvent(...)`；`styles.css:252-259` 行高 `9px 12px + 13px` ≈ 38px。
影响：Tab 走不到任何一行，键盘用户无法完成「换个去向」；移动端点击区低于 44px 容易误触。这是主路径上的阻断，不只是体验瑕疵。
标准：`quality-gates.md` 硬门槛（键盘焦点缺失／触摸目标不足）。
修改：优先换用 Obsidian 原生组件——`FolderPickerModal extends SuggestModal<string>`，键盘导航、模糊搜索、44px 行高、主题样式全部免费；`CategoryPickerModal` 只有 ≤12 项，改用 `Menu` 或 6 格按钮网格。若坚持自绘，必须 `role="button" tabindex="0"` + Enter/Space + `:focus-visible` + `min-height:44px`。
复测：仅用键盘完成「状态栏 → 弹窗 → 换个去向 → 选定」，移动端模拟器实测点击区 ≥44px。

### P1（系统性缺陷）

**P1-1｜状态栏的常驻前缀与不可聚焦**
现象：`main.ts:188` 固定输出 `JEV · ${text}`；`main.ts:161` 只在 `span` 上挂 `click`；`setStatus` 会 `addClass("is-ok")` 但 `styles.css` 只定义了 `is-busy / is-warn / is-error`。
影响：「JEV ·」在每个状态里重复，挤占本就宝贵的状态栏；`is-ok` 没有样式，成功与就绪只能靠小圆点区分；span 无 `role/tabindex`，键盘用户无法打开判断详情。
修改：前缀只在就绪态出现（`JEV`）；结果态直接给「待办 48%」；补 `.is-ok` 的规则；状态栏项改为可聚焦控件并带 `aria-label="查看最近一次判断详情"`；busy 的呼吸动画加 `prefers-reduced-motion` 降级。
复测：Tab 能聚焦状态栏项并用 Enter 打开弹窗；开系统「减少动效」后不再呼吸。

**P1-2｜弹窗内三个区域同底色，没有主角**
现象：`.jev-verdict`、`.jev-meta-cell`、`.jev-prob-track` 分别使用 `--background-secondary`，`modals.ts:54-80` 把 7 项元信息摊成等权卡片。
影响：扫视时没有第一落点，「去哪」这个真正结论被 6 张同色卡片稀释；用户需要逐个读完才知道该做什么。
修改：结论行去底色，改为一行「分类 → 目标文件夹」（唯一的分类色标识点 + 主字号 + 半粗）；元信息压成一行键值（置信度／门槛／领先优势／要求／长期价值），模型·时间·token 降到一行 `--text-muted` 尾注。卡片数量从 7 张降到 0 张，视觉层级由字号字重和发丝线承担。
复测：模糊（squint）测试下第一眼应落在结论行；灰度下仍能凭大小与字重分辨三级信息。

**P1-3｜概率图 6 色齐发 + 列宽写死**
现象：`styles.css:178` `grid-template-columns: 18px 68px 1fr 44px`；每个分类的键与条都用各自的饱和色。
影响：6 种高饱和色同屏，颜色失去「强调」含义；固定 px 列不随 Obsidian 的「字体大小」设置与 200% 缩放变化，字号调大后标签被 ellipsis 截断甚至挤爆。
修改：仅被选中行的条上色，其余条统一 `--background-modifier-border`（区分手段 = 条长 + 字重 + 颜色，不再只靠颜色）；列宽改 `16px max-content minmax(60px,1fr) 42px`；行高 26px 并加 `min-height` 防抖动。
复测：把 Obsidian 字体调大两档、浏览器缩放到 200%，逐行检查无截断、无重叠。

**P1-4｜四个同级按钮，主操作不点名对象**
现象：`modals.ts:103-130` 一次平铺 4 个按钮，主按钮文案是「移动到目标文件夹」。
影响：「重新判断」「移除判断块」与主操作同权重，违反「每页一个最强主操作」；用户到点击那一刻才知道目标文件夹是哪个。
修改：主按钮改为 `移动到 要做的事`（点名对象）；「换个去向…」保留为次级；「重新判断」「移除判断块」收进右侧 `⋯` 图标按钮（Obsidian `Menu`）。`移除判断块` 属可撤销但非必要动作，菜单项文案改「移除判断信息块」。
复测：弹窗内只有一个 CTA；Tab 顺序为 CTA → 次级 → ⋯。

**P1-5｜重置类动作语义重叠且无确认**
现象：`settings-tab.ts:175-199` 三个按钮并排（「恢复默认分类」「恢复默认分类名称与描述」「新增分类」）；`settings-tab.ts:628-641` 「恢复全部默认设置」一键清空含 API Key 在内的全部配置，只有 `setWarning()` 与 desc。
影响：「恢复默认分类」的 desc 缺失，用户不知道它会连文件夹一起覆盖；清空 API Key 不可逆且没有二次确认，误点成本高。
修改：三者合并为节末一个 `⋯` 菜单，重命名为「重置为默认分类（含文件夹）」「只重置名称与描述」「恢复全部设置（含 API Key）」；后两项（含重置分类）走 Obsidian `Modal` 二次确认，确认文案写明受影响对象与后果，确认按钮用警示样式并写动词（「重置分类」）。
复测：点击重置类动作必须先出现确认；取消后配置无变化。

**P1-6｜展开方式不可发现，禁用态用透明度砍对比度**
现象：`settings-tab.ts:243-259` 用文字按钮「展开 / 收起」并在折叠态额外绑定 `dblclick`；`styles.css:320-322` `.is-disabled { opacity: .5 }`。
影响：双击无任何视觉暗示，用户不会试；`opacity:.5` 把整卡文字压到过不了对比度的水平，同时让「停用」和「不相关」两种含义混在一起。
修改：头部整行改为真正的 `<button>`，左侧 `chevron-right` 图标 150ms 旋转，带 `aria-expanded`；移除 `dblclick`；禁用态改为仅把分类名降级为 `--text-muted`、标识点转灰，开关本身保持全对比度可见。
复测：键盘可展开／折叠；禁用卡内文字实测 ≥4.5:1。

**P1-7｜自动分流后没有就地撤销**
现象：`main.ts:415-424` 自动分流成功只发 `Notice`，撤销只能去命令面板找「撤销上一次分流」。
影响：后台替用户移动了文件却不在原地提供退路，是高后果动作的恢复缺口。
修改：分流成功的 `Notice` 之后，在状态栏结果态追加「已移动 · 撤销」的可点后缀（复用 P1-1 的可聚焦项），或在笔记内判断块 callout 里加一行「撤销上次分流」链接。撤销入口需在 12s 的 `statusBarClearMs` 窗口内可用。
复测：自动分流后 3 秒内点状态栏可撤销；撤销后文件回到原位。

**P1-8｜关键路径信息用 `--text-faint`**
现象：`.jev-picker-sub`（`styles.css:274-279`）与 `.jev-cat-folder-preview` 用 `--text-faint` 承载目标文件夹。
影响：暗色主题下实算 **2.90:1**，11px 文字远低于 4.5:1；而「去哪」恰恰是这个插件要回答的唯一问题。
修改：这类信息升到 `--text-muted`（暗色 7.95:1 / 亮色 6.69:1）；`--text-faint` 只留给真正可省略的装饰性内容。
复测：重跑对比度脚本，路径文字两主题均 ≥4.5:1。

### P2（打磨项）

| 编号 | 现象 | 修改 |
|---|---|---|
| P2-1 | 三个弹窗标题机制不一致：`modals.ts:36` 在内容区建 `h2`，另两个用 `titleEl` | 统一用 `titleEl`（Obsidian 内置 `.modal-title`），内容区不再重复标题 |
| P2-2 | `modals.ts:192` 裸 `createEl("input")`，与设置页输入框高度／聚焦环不一致 | 加 `.text-input` 类；聚焦用 Modal 生命周期而非 `setTimeout(focus, 50)` |
| P2-3 | 分类选择器无空状态（全部停用 → 空白弹窗） | 补 `.jev-empty` 同款空态文案：「没有启用中的分类，请先在设置里启用」 |
| P2-4 | `.callout[data-callout="jev-route"]` 硬编码 `92, 141, 255`，不跟随主题强调色 | 改用主题变量，或降低饱和度让 callout 与正文层级更近 |
| P2-5 | 颜色输入框只显示色块，无 HEX 值 | 色块旁显示 `#RRGGBB` 文本，非法值给内联提示 |
| P2-6 | 圆角混用 `--radius-m`（结论条／卡片／列表）与 `--radius-s`（元信息格／进度条） | 收敛为两级：容器 `--radius-m`，控件与条 `--radius-s`；同一功能不再混用 |
| P2-7 | `--font-ui-smaller` 承载了太多不同性质的信息（元信息标签、概率行、路径） | 按角色定 3 档：正文 `--font-ui-small`、辅助 `--font-ui-smaller`、微注 10px 且只用 `--text-muted` |

---

## 四、修改顺序

1. **P0-1**（颜色折算）——一行 CSS + 去掉 `modals.ts:89` 的赋值，收益最大、成本最低，先做。
2. **P0-2**（选择器换 `SuggestModal` / `Menu`）——删代码换可访问性，顺带解决 P2-2、P2-3。
3. **P1-2 + P1-3 + P1-4**（弹窗信息层级重构）——一次改完，因为三者共享同一套间距与层级规则。
4. **P1-5 + P1-6**（设置页控件与确认）——同一批 `settings-tab.ts` 改动。
5. **P1-1 + P1-7**（状态栏与撤销入口）。
6. 剩余 P1-8、P2 逐条收尾。

## 五、复测矩阵

| 改动区 | 受影响路径／视口／状态 | 期望结果 | 实测 | 回归检查 |
|---|---|---|---|---|
| 分类色折算 | 判断弹窗 · 亮/暗 · 6 个分类 | 图形位 ≥3:1，文字不再用分类色 | 待实测 | 设置页色块与笔记内 callout 不串色 |
| 选择器 | 状态栏 → 弹窗 → 换个去向（键盘 + 375/768px） | 纯键盘可完成；行高 ≥44px | 待实测 | 右键菜单里的「移动到指定文件夹…」同样可达 |
| 弹窗层级 | 判断弹窗 · 空 ranking · 长文件夹路径 | 第一眼落在结论行；路径不撑破布局 | 待实测 | 无 ranking 时不塌陷 |
| 设置页 | 分类卡展开／禁用／重置 | 键盘可展开；重置必确认；禁用态文字达标 | 待实测 | `MAX_CATEGORIES`、`至少保留 2 个分类` 提示仍生效 |
| 状态栏 | 就绪／忙碌／成功／警告／失败／减少动效 | 六态可辨；可聚焦可激活 | 待实测 | `statusBarEnabled` 关闭后不残留节点 |
| 撤销入口 | 自动分流 → 12s 内撤销 | 文件回原位 | 待实测 | 超过 10 条撤销记录后仍不报错 |

**停止条件**：两条 P0 关闭、硬门槛全绿、P1 全部修复或由你显式接受其后果后，再讨论是否继续打磨 P2。
